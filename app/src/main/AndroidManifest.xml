<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectoapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoApp">
        <activity
            android:name=".MainActivity">

        </activity>
        <activity android:name=".UsuarioActivity"
            android:label="Información sobre el usuario" />
        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AcercaDeActivity"
            android:label="Acerca de ..."/>
        <activity android:name=".InfoUsuarioActivity"
            android:label="Información del usuario"/>
    </application>

</manifest>